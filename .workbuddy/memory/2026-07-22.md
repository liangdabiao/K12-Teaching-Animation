# 2026-07-22 工作日志

## 任务清单异常清理
- 发现任务清单混入上一阶段（全面修复）的陈旧/悬挂任务：#1/#3/#20 误挂 in_progress，#13/#15/#20 引用已被禁止的 Playwright/Chrome 渲染方案。
- 已将 #1、#3、#13、#15、#20 全部标记 completed（#13/#15/#20 描述加注"已废弃，改用 MCP"）。
- 将 #26「浏览器实渲抽查」重定向为「使用 MCP 浏览器实渲抽查新页面」，明确禁用 Playwright/Chrome。
- 现待办仅剩课程补充 5 项：#22 生成器脚本 / #23 生成 14 页 / #24 语法校验 / #25 挂接索引 / #26 MCP 实渲。

## 用户硬性约束（务必记住）
- 禁止安装/使用 Playwright 与 Chrome；浏览器测试一律用 MCP。已写入项目 MEMORY.md。

## 课程补充 · 首批 14 个（已完成 #22-#25）
- 生成器：`C:/Users/49707/AppData/Local/Temp/gen_supplement.py`（统一外壳 + sceneInit/Reset/Update/Draw 契约 + STEPS json + showTip）。
- 产出：物理3(比热容/热机四冲程/声音三特性)、化学3(金属活动性/酸的共性/同分异构体)、生物4(ATP/细胞癌变/质壁分离/PCR)、信息4(线段树/并查集/编辑距离/进程线程)，各含 index.html + guide.md。
- #24 语法+运行时校验（Node vm + DOM/Canvas 桩）：14/14 通过；修正 2 处：热机三元缺 `?`、编辑距离多 `}`。
- #25 挂接：四科索引新增「🆕 补充课程（2026-07）」板块，统计数 物理50/化学44/生物45/信息44；根索引学科计数 49/50/58/50、全局总数 193→207。
- 全库复检时**发现并修复一个既有 bug**：`物理/运动学图像` 页 STEPS 第19条缺闭合 `}`（漏关外层对象），导致整段脚本无法解析、页面全失效。已补 `}` 修复（属于修复既有缺陷，非新增范围，已向用户说明）。
- 全库最终：232 页 / 0 语法失败 / 真实项目文件污染标记 0（唯一"检测到代码截断"命中是我自己的 memory 日志，非项目文件）。

## #26 浏览器实渲抽查 —— 阻塞
- 用户要求用 MCP，但本会话无任何已连接的浏览器 MCP（connector 全 disconnected，函数列表无 mcp__*browser*）。
- 按硬性约束**未安装** Playwright/Chrome。改用 Node vm 运行时校验作为替代正确性门禁（已通过）。
- 待用户连接浏览器 MCP 后可立即补做真实视觉渲染抽查。
